import { FormScreenContainer } from "../Form/styles";
import styled from 'styled-components';
export const PinEnterContainer=styled(FormScreenContainer)``;

export const PinEnterTextContainer=styled.div`
    text-align:center;
    font-size:20px;
`;
